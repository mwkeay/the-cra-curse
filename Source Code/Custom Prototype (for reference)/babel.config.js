const config = {

    presets: [
        
        // Handles modern JavaScript features
        "@babel/preset-env",

        // Handles JSX syntax transformation
        "@babel/preset-react",
    ],
};

module.exports = config;
